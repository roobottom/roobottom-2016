{% macro c_siteHeader(items,active) %}
  <header class="c_siteHeader {% if active %}c_siteHeader--{{active}}{% endif %}">
    {% for item in items %}
    {{item}}
    {% endfor %}
  </header>
{% endmacro %}
