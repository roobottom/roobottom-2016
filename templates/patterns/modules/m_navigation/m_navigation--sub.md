Passing the modifier `sub` to `m_navigation` will render an in-line sub navigation.

```
pattern.m_navigation([
    {name:'Jedi',url:'#'},
    {name:'Mind',url:'#'},
    {name:'Tricks',url:'#'}
  ],active='tricks',modifier='sub')
```
