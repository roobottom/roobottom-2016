{% macro m_pageTitle(title,date) %}
<h1 class="m_pageTitle">{{title|capitalize|fixOrphans}}</h1>
{% if date %}
<time datetime="{{date}}" class="m_pageTitle__date">{{date | date}}</time>
{% endif %}
{% endmacro %}
