{% macro m_posts (posts) %}
  {% for post in posts %}
    {% if post.attributes.type == 'diary' %}
      {{ m_postDiary (post) }}
    {% endif %}
    {% if post.attributes.type == 'gallery' %}
      {{ m_postGallery (post) }}
    {% endif %}
    {% if post.attributes.type == 'notes' %}
      {{ m_postNote(post) }}
    {% endif %}
  {% endfor %}
{% endmacro %}
